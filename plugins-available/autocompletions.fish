# complete -x -c jira -a '(jira shell --auto)'
complete -x -c jira -a '(/Users/galzsolt/MySites/jira/bin/jira shell)'

# for command in (/Users/galzsolt/MySites/jira/bin/jira shell)
    # complete -x -c "jira" -c "$command" -a 
# end